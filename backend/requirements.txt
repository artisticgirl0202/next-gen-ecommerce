# =============================================================================
# Core production dependencies
# Deploy target: Render free tier (512 MB RAM)
#
# ML / AI heavy packages live in requirements-ml.txt.
# DO NOT add torch, faiss, sentence-transformers here — they alone exceed 512MB.
# The recommend router gracefully falls back to Postgres RANDOM() when the ML
# stack is absent, so the rest of the app works normally without them.
# =============================================================================

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.110.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ── Database ─────────────────────────────────────────────────────────────────
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
asyncpg==0.29.0

# ── Validation / serialisation ────────────────────────────────────────────────
pydantic==2.6.4
# email-validator must be >=2.0.0 for Pydantic V2 EmailStr support.
# Without this pin, pip may install 1.x which silently breaks EmailStr → 422.
email-validator>=2.0.0

# ── Auth & security ───────────────────────────────────────────────────────────
passlib[argon2]
python-jose[cryptography]
google-auth

# ── Environment & config ──────────────────────────────────────────────────────
python-dotenv==1.2.1

# ── Cache / messaging ─────────────────────────────────────────────────────────
redis==5.0.1
aiokafka==0.11.0
aiofiles==23.2.1
kafka-python==2.3.0

# ── HTTP client (Google OAuth userinfo endpoint) ──────────────────────────────
requests==2.32.5

# ── Numerics (lightweight — needed for recommend fallback math) ───────────────
numpy==1.26.4
scikit-learn==1.4.2
scipy==1.16.3
joblib==1.5.3

# ── Utilities ─────────────────────────────────────────────────────────────────
orjson==3.9.15
Jinja2==3.1.6
